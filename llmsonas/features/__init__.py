"""Feature engineering — turns each user's review record into a behavioural
vector (a numeric block, plus a text-stance embedding when the source ships
review text) used for stratified sampling, clustering, and graph similarity."""
