"""Survey execution — poses the single-select question in the third person and
reads the answer from option-token logprobs (not free text), recovering a
per-persona probability rather than a sampled hard vote."""
