"""Run harness.

Every method/question pass goes through one entry point,
``run(method, question) -> {p_hat, ci, jsd, swing_err, ...}``, executed for
M1, M2a, M2b and M3 so the deliverable is a single methods x questions table.
Implemented once the stage modules land.
"""
