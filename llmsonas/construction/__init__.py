"""Persona construction — selecting the population and writing its prompts.

Two grounded methods share this module:
  M2a  stratified sample of real users (each persona = one person).
  M2b  cluster archetypes weighted to real cluster shares.
Both end in a deterministic third-person biographical profile (no demographic
invention) to pre-empt social-desirability triggers at survey time.
"""
